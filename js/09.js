// Booleans

const boolean1 = true; // Esta aplicando la funcion de booleano en este caso boolean1 es verdadero.
const boolean2 = false; // Esta aplicando la funcion de booleano en este caso boolean2 es falso.
//const boolean3 = "true"; // En este caso no se esta aplicando la funcion de booleano en boolean3 se considera como un String.

console.log(boolean1);
console.log(boolean2);
//console.log(boolean3);

const boolean3 = new Boolean(true); // Otra forma de como trabajar con los booleanos, pero en esta caso se considera mas como objeto que booleano.
console.log(boolean3);
